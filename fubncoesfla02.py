# Lista Funções

Implemente as funções no arquivo `main.py`.

Crie pelo menos mais um caso de teste para cada problema. Veja os exemplos no final do arquivo.

Aperte o botão `run` para testar.

<hr></hr>

## Funções

**01** Escreva uma função que retorne o maior de 2 números. O nome da função como especificado abaixo:

```
maior2(a, b: int)
  # Corpo da função
```

**02** Escreva uma função que retorne o maior de 5 números. O nome da função deve ser obrigatoriamente maior5, como especificado abaixo:

```
maior5 (a, b, c, d, f: int)
  # Corpo da função
```

**03** Escreva uma função, chamada quantidade_de_vogais, que recebe um texto e retorna a quantidade de vogais do texto.

```
quantidade_de_vogais(s: str)
  # Corpo da função
```

**04** Escreva uma função, chamada impar que recebe um valor e retorna verdadeiro, se o número for ímpar, caso contrário a função retorna falso.

```
impar(n: int)
  # Corpo da função
```

**05** Sabendo que palavras de uma frase são separadas por espaços, escreva uma função que retorne a quantidade de palavras de um texto.

```
conta_palavras(texto: str)
  # Corpo da função
```

**06** Escreva uma função que recebe dois número inteiros, a e b, e retorne uma lista com todos os números entre a e b, inclusive.

```
intervaloab(n1, n2: int)
  # Corpo da função
```

**07** Escreva uma função que recebe um número e retorna o fatorial dele.

```
fatorial(n: int)
  # Corpo da função
```

**08** Escreva uma função que recebe um número inteiro e retorna uma lista com os divisores do número, incluindo o número 1 e ele mesmo.

```
divisores(n: int)
  # Corpo da função
```

**09** Escreva uma função que receba um número inteiro e verifique se o mesmo é um número primo, retornando verdadeiro ou falso.

```
primo(n: int)
  # Corpo da função
```

**10** Escreva uma função que recebe dois números inteiros e retorna verdadeiro, se eles são primos entre si, ou falso, caso contrário.

```
primos_entre_si(n1, n2: int)
  # Corpo da função
```

**11** Escreva uma função que calcule o MDC (Máximo Divisor Comum) de dois números dados.

```
mdc(n1, n2: int)
  # Corpo da função
```

**12** Escreva uma função que, dado dois números, retorne o MMC dos dois.

```
mmc (n1, n2: int)
  # Corpo da função
```

**13** Escreva uma função, chamada transcreve_numero(telefone), que transcreva os dígitos de um dado número de telefone. A função deve receber um texto contendo um número telefônico e deve retornar uma lista dos nomes dos dígitos. Qualquer caractere que não seja número deve ser ignorado. Considerando que o conteúdo do texto passado seja `"3221-1234"` a função deve retornar a lista `["tres", "dois", "dois", "um", "um", "dois", "tres", "quatro"]`.

```
transcreve_numero(telefone: str)
  # Corpo da função
```

**14** Escreva uma função que receba duas listas e identifique as coincidências entre elas, ou seja, os elementos que se encontram na mesma posição (linha e coluna). A função deve retornar uma nova lista contendo os elementos que coincidem nas suas posições e 0 (zero) nas demais posições. Todas as listas recebidas pela função e a lista retornada devem ter o mesmo número de linhas e colunas.

```
coincidencia_lista(lista1, lista2: List[int])
  # Corpo da função
```